project(test-regrule)
compiler(g++)
linker(g++)
source_dir("src")
source_exts("cpp")
build_dir("build")
compiler_flags(" -std=c++14 -Wall")
linker_flags(" -s")
libraries("boost_filesystem boost_system")
